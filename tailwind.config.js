// Tailwind CSS v4 does not use this file by default (config is in CSS). 
// Keeping purely as fallback or for extensions if needed, but standard v4 uses CSS variables.
// Leaving empty or minimal.
export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}
